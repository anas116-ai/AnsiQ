# AnsiQ Prometheus Alerting Rules

# groups:
#   - name: AnsiQAlerts
#     interval: 30s
#     rules:
#       - alert: HighErrorRate
#         expr: rate(http_requests_total{status=~"5.."}[5m]) > 0.05
#         for: 5m
#         labels:
#           severity: critical
#         annotations:
#           summary: High error rate ({{  | humanizePercentage }})
#       - alert: HighLatency
#         expr: histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m])) > 2.0
#         for: 5m
#         labels:
#           severity: warning
#         annotations:
#           summary: P95 latency above 2s
